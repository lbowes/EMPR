#include "Motion.h"

int main()
{
    // Motion_init();
    // Motion_moveAxisToPos(EMPR_X_AXIS, 200);

    // uint8_t count = 0;
    // for(count = 0; count < 199; count++) {
    //     Motion_moveAxisToPos(EMPR_X_AXIS, count);
    //     Motion_moveAxisToPos(EMPR_Y_AXIS, count);
    // }

    // Motion_neutralise(EMPR_X_AXIS);
    StreamSimpleScan();
    return 0;
}